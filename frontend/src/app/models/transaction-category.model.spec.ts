import { TransactionCategory } from './transaction-category.model';

describe('TransactionCategory', () => {
  it('should create an instance', () => {
    expect(new TransactionCategory()).toBeTruthy();
  });
});
